Тестовое задание
===============

Это простое тестовое задание на работу с транзакциями в БД.

Запуск
----------

Точка входа в `public/index.php`.
Можно запустить в директории `public` встроенный в PHP сервер: `php -S localhost:8000`

По-умолчанию подключение в БД осуществляется к базе `test_project` с данными `root`:`root`.
